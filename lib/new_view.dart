import 'package:flutter/material.dart';

class NewViews extends StatefulWidget {
  @override
  createState() => new NewViewsState();
}

class NewViewsState extends State<NewViews> {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text('New View'),
      ),
      body: null,
    );
  }
}